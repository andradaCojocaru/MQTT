apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: "${INFLUXDB_URL}"
    jsonData:
      version: Flux
      defaultBucket: "${INFLUXDB_BUCKET}"
      organization: "${INFLUXDB_ORG}"
    secureJsonData:
      token: "${INFLUXDB_TOKEN}"